name: sxopop
description: SXOPOP - Chat. Share. Connect.
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.5.0
  firebase_storage: ^12.3.0
  http: ^1.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
